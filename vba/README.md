# VBA source

**Current status: VBA PROHIBITED.**

Do not add modules until `docs/workbook-contract.md` says `FROZEN FOR VBA` and the owner has approved the macro-free workbook.

When authorized, export every standard module, class module, form, worksheet code-behind module, and `ThisWorkbook` module as source-controlled text where technically possible. Each module requires a matching test/evidence record.
