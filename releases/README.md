# Approved releases

Only approved, tested release packages belong here. Each release must include a manifest with:

- semantic version;
- Git commit SHA;
- workbook contract version;
- SHA-256 checksum;
- Excel version/build tested;
- test summary;
- scanner configuration tested, if available;
- known limitations;
- deployment and rollback instructions.
